meta {
  name: Remove User From Organization
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/organizations/remove-user
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "userId": "USER_ID_HERE"
  }
}
