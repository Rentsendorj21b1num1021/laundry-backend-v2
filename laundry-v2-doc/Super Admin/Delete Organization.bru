meta {
  name: Delete Organization
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/admin/organizations/ORG_ID_HERE
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "confirmDelete": true
  }
}
