meta {
  name: Delete Order
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/deleteOrder
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "orderId": "ORDER_ID_HERE"
  }
}
